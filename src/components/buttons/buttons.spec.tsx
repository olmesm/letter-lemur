import * as React from "react";

import { Button } from "./index";

describe("<Button />", () => {
  test("renders all children", () => {});
  test("adds a css class if required", () => {});
  test("applies onClick event", () => {});
  test("applies href and anchor if defined", () => {});
  test("applies href and anchor if defined", () => {});
});
